from setuptools import setup
from setuptools import find_packages

setup(
    name='loki',
    version='0.0.1',
    author='Pedro Bueno',
    description='An Operator to Loki',
    packages=find_packages()
)
